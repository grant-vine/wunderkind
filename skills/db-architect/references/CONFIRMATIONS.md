# Destructive Action Confirmations

Append approved destructive database operations here using the format required by `skills/db-architect/SKILL.md`:

`## [YYYY-MM-DD] [OPERATION_TYPE] on [TARGET] — APPROVED`

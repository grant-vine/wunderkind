# Learnings — openagent-migration-and-plan-restructure

## [2026-03-10] Session: Atlas orchestrator initialized

### Key Repo Facts
- Package: `@grant-vine/wunderkind` v0.5.0 → target `0.7.0`
- `agents/` is GENERATED by `bun run build` — never hand-edit
- `skills/` is STATIC source — hand-authored SKILL.md files
- Two-step build: `tsc` then `bun run dist/build-agents.js`
- Tests: `bun test tests/unit/` in `tests/unit/` dir

### Brand/Technical Split (CRITICAL)
- PRESERVE LITERAL: `oh-my-opencode` (npm package name), `bunx oh-my-opencode` (binary), `oh-my-opencode.jsonc` (config filename), `oh-my-opencode.schema.json` (schema filename)
- RENAME to `oh-my-openagent`: repo/documentation URLs from `github.com/code-yeongyu/oh-my-opencode` → `github.com/code-yeongyu/oh-my-openagent`
- USER-FACING BRAND: use `oh-my-openagent` in descriptive/prose text
- Upstream package name still `oh-my-opencode` on npm — do NOT rename dependency

### Category Mapping for oh-my-opencode.jsonc
- marketing, product, brand, devrel, legal, support, data-analyst → `writing`
- fullstack, qa, operations, ciso → `unspecified-high`
- creative-director → `visual-engineering`

### Upstream Category Defaults (from planning research)
- `quick` → `anthropic/claude-haiku-4-5`
- `unspecified-low` → `anthropic/claude-sonnet-4-6`
- `unspecified-high` → `openai/gpt-5.4` with `variant: high`
- `writing` → `google/gemini-3-flash`
- `visual-engineering` → `google/gemini-3.1-pro` with `variant: high`

### TypeScript Flags (CRITICAL)
- `exactOptionalPropertyTypes: true` — most common source of type errors
- `noUncheckedIndexedAccess: true`
- `noUnusedLocals: true`, `noUnusedParameters: true`
- No `as any`, `@ts-ignore`, `@ts-expect-error` allowed

### Commit Strategy
- Commit A (after Tasks 2-4): `feat(branding): adopt openagent brand-first wording for pre-1.0 release`
- Commit B (after Tasks 5-7): `refactor(config): inherit upstream categories and regenerate agent prompts`
- Commit C (after Tasks 8-10): `docs(plans): decompose docs-output mega-plan into workstreams`

## [2026-03-10] Task 6 targeted contract tests

- Added a manifest sync unit test that reads `package.json` and `.claude-plugin/plugin.json` directly and locks both versions to `0.7.0`.
- Added a JSONC config-template contract test using `jsonc-parser`; it asserts all 12 `wunderkind:*` agents exist, each uses `category`, and none define per-agent `model` keys.
- Added CLI help-copy assertions that verify the current `oh-my-openagent` branding and revised installer wording without requiring older technical identifiers to disappear everywhere.
- Task 10: Added 'Execution Guardrails' and crosswalk to the superseded 'docs-output-system.md'. This prevents accidental misuse of the old plan while preserving the mapping to new workstreams D1-D4.

## Task 7: Build Regeneration & Brand Audit (2026-03-10)

- `bun run build` (tsc + bun run dist/build-agents.js) exits 0 cleanly after Task 4-6 source changes
- All 12 `agents/*.md` files regenerated successfully; sizes range 10952–15612 bytes
- Brand audit PASSED: zero occurrences of `oh-my-opencode workflow` in generated files; exactly 12 occurrences of `oh-my-openagent workflow` (one per agent file)
- Technical literal `wunderkind.config.jsonc` appears 21 times across generated files — preserved as expected
- `tsc --noEmit` exits 0 — no type regressions from the migration
- Evidence written to `.sisyphus/evidence/task-7-build.txt` and `.sisyphus/evidence/task-7-generated-brand-audit.txt`

## Task 11: Final Repo-Wide Contract Sweep (2026-03-10)

### All 10 verification checks PASSED — repo is clean and contract-complete

**Results summary:**
1. `tsc --noEmit` → exit 0, 0 errors
2. `bun test tests/unit/` → 62/62 passing across 5 files
3. `bun run build` → 12 agents generated, exit 0
4. Manifest versions → package.json=0.7.0, plugin.json=0.7.0 ✅
5. oh-my-opencode dependency → `^3.11.0` ✅
6. Old repo URL audit → 0 occurrences of `github.com/code-yeongyu/oh-my-opencode` in all maintained files ✅
7. Breaking change note → README line 8 confirmed present ✅
8. Stale 8-agent wording → 0 occurrences in README.md, AGENTS.md, src/ ✅
9. Config inheritance → 12 agents, all with category, zero per-agent model keys ✅
10. docs-output-system.md → 0 unchecked TODOs ✅

### Key confirmation: The 12-agent expansion is fully coherent
The build produces exactly 12 agent files (added in Tasks 5-6: devrel-wunderkind, legal-counsel, data-analyst, support-engineer to the original 8). All are indexed in README, oh-my-opencode.jsonc, and AGENTS.md with no stale "8 agent" references remaining.

### Evidence files
- `.sisyphus/evidence/task-11-full-verification.txt` — all 10 check outputs
- `.sisyphus/evidence/task-11-leftover-audit.txt` — checks 6, 8, 9 detail

## [2026-03-10] F4 scope fidelity adjudication

- Re-audited commits d166216, b3a35b0, 00d7230, 331aaea, b8965e2 against Tasks 1-11 and docs-output deferred/cancelled buckets.
- Confirmed remediation commit b8965e2 is in scope: AGENTS.md + README.md fixes complete Task 3 contract; tests/unit/bun-test.d.ts + tests/tsconfig.json address F2 bun:test/LSP type-resolution gaps.
- Confirmed no deferred/cancelled docs-output implementations leaked (no doctor command, configVersion/upgrade logic, personality gate, or docs runtime feature work landed).
- Confirmed plan-file edits in .sisyphus/plans/openagent-migration-and-plan-restructure.md are orchestrator tracking activity and must not trigger F4 rejection.
- Final F4 verdict recorded as APPROVE in evidence file.


## [2026-03-10] F1 final approval basis

- For this plan, `package.json` discoverability keywords are allowed preserve-literal metadata when explicitly clarified by the orchestrator; they are not automatically user-facing rename targets.
- Commit-boundary review should treat the plan’s A/B/C grouping as logical task grouping, not strictly chronological ordering, and should allow pure `.sisyphus/` orchestration/evidence commits plus post-review remediation commits when they only close plan-deliverable gaps.

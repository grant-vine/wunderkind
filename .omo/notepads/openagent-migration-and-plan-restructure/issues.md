# Issues — openagent-migration-and-plan-restructure

## [2026-03-10] Session: Atlas orchestrator initialized

### Known Gotchas
- `agents/` is gitignored but published via `"files"` in package.json — generated by bun run build
- `.claude-plugin/plugin.json` version must be manually kept in sync with package.json (no automation)
- JSONC comments are lost on opencode config write (JSON.stringify used)
- `createRequire` in src/cli/index.ts is the only CommonJS interop — used to read package.json at runtime
- Legacy `wunderkind.config.jsonc` at project root causes error + exit 1 (no auto-migration)
- OpenCode config path is `~/.config/opencode/opencode.json` (not legacy config.json)
- oh-my-opencode must be installed before wunderkind

### Task Execution Notes
(to be updated as tasks complete)

## [2026-03-10] Task 1 inventory findings

- Inventory scope produced 49 grep-hit lines and 59 exact `oh-my-opencode|oh-my-openagent` occurrences across package metadata, docs, CLI text, agent prompts, and config comments.
- The four hard-preserve literals were confirmed explicitly: npm dependency `oh-my-opencode`, install command `bunx oh-my-opencode`, config filename `oh-my-opencode.jsonc`, schema filename `oh-my-opencode.schema.json`.
- Mixed-content lines need token-level handling during migration: several lines contain both a brand string and a repo URL, and schema URLs contain both a repo-path rename and a preserved schema filename.
- README still contains obsolete runtime `oh-my-opencode.json` references; matrix classification marks those as DELETE rather than rename/preserve.
- [2026-03-10] Appended crosswalk table to docs-output-system.md. Classified all 38 unchecked TODOs into D1, D2, D3, D4, DEFERRED, and CANCELLED buckets as requested by Task 8.

## [2026-03-10] Task 5 gotchas

- Running the JSONC parse verification via `node --input-type=module` initially failed because workspace dependencies were not installed (`ERR_MODULE_NOT_FOUND` for `jsonc-parser`); resolved by running `bun install` before verification.
- `lsp_diagnostics` on `oh-my-opencode.jsonc` initially failed because Biome was configured but missing in the environment; resolved by installing `@biomejs/biome` globally and re-running diagnostics successfully.

## [2026-03-10] Task 4 src branding sweep

- Updated `src/cli/index.ts` help prose from the old brand reference to `oh-my-openagent` and corrected the agent count to twelve.
- Updated all 12 `src/agents/*.ts` prompt strings from `oh-my-opencode workflow (Atlas/Sisyphus)` to `oh-my-openagent workflow (Atlas/Sisyphus)`.
- Checked `src/index.ts` for descriptive `oh-my-opencode` prose; no matching brand string remained there, so no source edit was required.
- Saved verification evidence to `.sisyphus/evidence/task-4-src-brand-audit.txt` and `.sisyphus/evidence/task-4-generated-guardrail.txt`.
- Verified clean `lsp_diagnostics` on changed files and passed `tsc --noEmit` after the wording-only edits.


## [2026-03-10] F1 rerun findings

- `package.json` still contains the old-brand keyword `oh-my-opencode`, which conflicts with the Task 1 matrix row that classified that token as `USER_FACING_BRAND_RENAME`.
- Docs-plan grouping is still split in git history: `00d7230` carries `.sisyphus/plans/docs-output-system.md`, while `331aaea` reuses the Commit C message for evidence/orchestration files instead of the docs-plan file itself.
